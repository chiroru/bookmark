!!!
%html
  %head
    %title #{@title}
  %body
    #header
    %h1 Bookmark
    #content
    %p
      - @entries.each do |entry|
        %li
          %a{ href: entry.url } #{entry.url}
          (#{timefmt(entry.created_at)})
      %form{ action: "/add", method: "post"}
        %input{ type: "textfield", name: "url" }
        %input{ type: "submit", value: "send" } 
